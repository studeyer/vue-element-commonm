import Vue from 'vue'
import manageVersion from '../page/manageVersion'

const routes = [
		{
      path: '/',
      name: 'manageVersion',
      component: manageVersion
    }
]
export default routes
